---
label: Homepage
hide_body: true
fields:
- type: text
  name: name
  label: Name
  hidden: true
- type: text
  name: description
  label: SEO Description
  description: A short sentence describing the page to Google.
  config:
    required: true
- type: field_group_list
  name: slides
  fields:
  - type: text
    name: title
    label: Title
    description: Ex. Moeller House
    config:
      required: true
  - type: text
    name: details
    label: Details
    description: Ex. 2014 - Residential, Indianapolis
    config:
      required: true
  - type: text
    name: slug
    label: slug
  - type: file
    name: image
    label: Image
    description: Should be at least 1440 pixels wide. The image will always be centered,
      but the visible portion is based on user's screen size.
  label: Featured Projects
  config:
    labelField: title
- name: projects
  type: field_group_list
  fields:
  - name: project
    type: select
    default: []
    config:
      required: false
      options: []
      source:
        type: pages
        section: projects
        file: 
        path: 
    label: Project
  config:
    min: 
    max: 
    labelField: project
  label: Featured Projects
pages:
- hugo/content/_index.md
