{
  "settings": {{ .Site.Data.settings | jsonify }},
  "person": {
    "fullname": {{ .Params.fullname | jsonify }},
    "jobTitle": {{ .Params.jobTitle | jsonify }},
    "phone": {{ .Params.phone | jsonify }},
    "fax": {{ .Params.fax | jsonify }},
    "email": {{ .Params.email | jsonify }},
    "bio": {{ .RawContent | jsonify }}
  }
}